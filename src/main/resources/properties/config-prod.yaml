http:
  private:
    port: "8081"

sys:
  auth:
    clientId: "replace-prod-client-id"
  anypoint:
    protocol: "HTTPS"
    host: "replace-prod-anypoint-host"
    port: "443"
  observability:
    protocol: "HTTPS"
    host: "replace-prod-observability-host"
    port: "443"
  controlPlane:
    protocol: "HTTPS"
    host: "replace-prod-aip-mule-sys-control-plane-host"
    port: "443"
    mockMode: "false"

# Autodiscovery configuration
api.id: "0"
