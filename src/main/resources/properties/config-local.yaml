http:
  private:
    port: "8081"

sys:
  auth:
    clientId: "01115d3f8c8c42c5b66bb585baca652d"
  anypoint:
    protocol: "HTTP"
    host: "localhost"
    port: "8082"
  observability:
    protocol: "HTTP"
    host: "localhost"
    port: "8083"

control:
  plane:
    mockMode: "true"

# Autodiscovery configuration
api.id: "0"
