# Do not edit this file
# values will be replaced by Maven and used in jenkins(CI/CD)

application:
  name: "${project.name}"
  groupId: "${project.groupId}"
  artifactId: "${project.artifactId}"
  version: "${project.version}"
  runtime: "${app.runtime.version}"

api:
  name: "${api.name}"
  version: "${api.version}"
  versionId: "${api.versionId}"
  domain: "${api.domain}"
